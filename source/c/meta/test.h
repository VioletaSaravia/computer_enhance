#pragma once

#include "../lib/types.h"

GEN(array)
typedef struct Blabers {
    const char* yabbers;
    long        boobers, loobers;
    int         bleep;
} Blabers;

GEN(hoho)
typedef struct Foozz {
    cstr yabbers;
    GEN(slider) long boobers, loobers;
    int bleep;
} Blabers;